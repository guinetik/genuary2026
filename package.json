{
  "name": "genuary2026",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "lint:shaders": "node scripts/lint-shaders.js"
  },
  "dependencies": {
    "@guinetik/gcanvas": "1.0.5"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "gh-pages": "^6.1.0"
  }
}
